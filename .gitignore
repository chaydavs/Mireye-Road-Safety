# Data artifacts (cache, parquet, downloaded sources) — regenerated by the pipeline.
# Anchored to the repo root so it does NOT also ignore web/public/data (the app's committed JSON).
/data/

# Secrets
.env

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# Tooling noise
.gyst/
.hintrc

# Scratch (dev screenshots, playwright artifacts, OS files)
/subgrade-*.png
/ablation-*.png
.playwright-mcp/
.DS_Store

# Local dev tooling + regenerated artifacts (not part of the deliverable)
.claude/settings.json
.claude/settings.local.json
output/map.html
