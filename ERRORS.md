# AI error log

One line per caught error: what was wrong, how it was caught. No editorializing.

<!-- format: YYYY-MM-DD | session N | what was wrong | how it was caught -->

2026-07-15 | session 1 | Assumed `/v1/fetch` longitude param was `lon`; POST returned HTTP 422 "missing field: lng". Corrected to `lng`. | Mireye API request validation (422 body named the missing field).
2026-07-15 | session 1 | Assumed `/v1/fetch` was a GET with query params; bare GET returned 404. Endpoint is POST with a JSON body. | Discovery probe against the live API (404 on GET, 200 on POST).
2026-07-16 | session 1 | `fetch_point` retried a 429 and returned the retry's status, so `run_probe` only counted a rate-limit on a double-429; single recovered throttles were invisible ("429: 0" could be masking throttling). | code-reviewer subagent; fixed by returning a `saw_429` flag counted regardless of retry outcome.
2026-07-16 | session 1 | Re-running the probe on the same fixed 100 points measured Mireye's warm server-side cache (~0.13 s/call) not cold first-touch (~0.3–2 s), overstating sustainable throughput ~4-15x — would have made Session 2 wrongly conclude the county fits in minutes. | Caught by comparing run 1 (cold) vs re-runs (warm); fixed by jittering points per run so every run measures cold.
2026-07-16 | session 1 | `import random` was silently deleted by the PostToolUse `ruff --fix` hook because the import was added one edit before the code that used it (momentarily unused). | Next `ruff`/`pytest` failed with F821/NameError; re-added after its use existed.
2026-07-16 | session 2 | Picked VirginiaRoads `VA_Primary_and_Secondary_Roads` for centerlines; it has NO local (S1400) roads in Loudoun, which are the product's whole point. | Inspected the layer's MTFCC distribution (only S1100/S1200); switched centerlines to Census TIGER All Roads (FIPS 51107).
2026-07-16 | session 2 | Centerlines had ~9% exact-duplicate TIGER geometries; left in, a physical road would be segmented, scored, and mapped twice. | data-qa subagent flagged duplicate point geometries; fixed by deduping duplicate geometries at load (17,200→15,643 segments).
2026-07-16 | session 3 | Snap-QA rule discarded any point whose Mireye nearest_road_name != TIGER route_name; this discarded 28% of points that were the SAME road named differently (Overture "East Market Street" vs TIGER "State Rte 7 Bus"). | Inspected the 14 discards (all major arterials); switched QA to distance-based (discard only if far from any road), name mismatch now a kept low-confidence flag.
2026-07-16 | session 3 | `derive_status` only treated Mireye per-field status "ok" as non-failure, so status "absent" (semantic absence, e.g. no nearby waterbody) was mislabeled "failed" — inflating failure rates (nearest_waterbody_name 96%, flowline 64%). | data-qa flagged the high "failed" rates; inspected raw payloads (status="absent"); fixed derive_status to map "absent" → absent-semantic.
2026-07-16 | session 3 | fetch.py never validated field names against the LIVE catalog before fetching (reused probe's list from memory) — a CLAUDE.md CRITICAL rule; a stale QA field name would have silently made snap-QA a no-op. | code-reviewer subagent; added probe.fetch_catalog + validate_fields guard in fetch.main() before the loop.
2026-07-16 | session 3 | audit null-rate lumped absent-semantic (legit) with failed (data gap), and the kill criterion counted semantic absence toward the >40% stop — undoing the derive_status distinction. | code-reviewer; audit now emits per-field status_distribution and bases the kill on failed-rate only.
2026-07-16 | session 3 | Fetch loop hung ~7 hours at 0% CPU: one httpx.Client shared across 4 threads reused keepalive sockets that went stale when Mireye's fly.io app cycled; the per-request timeout never fired and httpcore's shared pool lock stalled all threads. | User flagged the 7-hr runtime; diagnosed via ps (4s CPU) + lsof (4 idle ESTABLISHED sockets + open sqlite journal). Fixed: per-thread clients, explicit connect/read timeout, 15s keepalive expiry.
2026-07-16 | session 3 | While fixing the hang, changed fetch_coord to take a token but left `_resnap_once` passing an httpx.Client to it — would have built a garbage "Bearer {httpx.Client}" token had any resnap fired. | Caught reading the diff during the refactor (resnaps were 0 in trials so it never executed); threaded token through run_qa/_resnap_once.
2026-07-16 | session 4 | Why-card emitted a FALSE cited claim: for segments using the housing-density proxy, it printed the housing number as "AADT = N vehicles/day (VDOT)" — a fabricated citation, violating the no-provenance-no-claim hard rule. | code-reviewer subagent; tagged traffic driver as traffic_aadt vs housing_units_density_per_km2, proxy now cited to its own Mireye provenance row. Added tests/test_why_card.py.
