# Data artifacts (cache, parquet, downloaded sources) — regenerated by the pipeline.
# Anchored to the repo root so it does NOT also ignore web/public/data (the app's committed JSON).
/data/

# Secrets
.env

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# Tooling noise
.gyst/
.hintrc

# Scratch (dev screenshots, playwright artifacts)
/subgrade-*.png
.playwright-mcp/
